# Keeps the SHA-pinned GitHub Actions in ci.yml up to date: Dependabot opens
# a PR bumping the pin (and its version comment) when an action releases.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
